{{all-items items=model.items}}
{{starred-items items=model.starred}}

{{#each model.unfoldered as |feed|}}
  {{feed-item feed=feed onClick=(action "transistionTo" feed)}}
{{/each}}

{{folder-list folders=model.folders onClick="selectFeed"}}

